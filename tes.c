#include<stdio.h>
int main()
{
    
    printf("Program Membuat Inventori");
    switch (pilihan)
    {
    case 1:
        
        break;
    
    default:
        break;
    }
    
    /*
    char data[50];
    int nim;
    FILE *fp;

    fp = fopen ("tes.txt", "w");
    for (int i=0; i<5; i++)
    {
        printf("Masukkan nama : ");
        fflush(stdin);
        gets(data);
        fprintf(fp, "%s\n", data);
        printf("Masukkan NIM : ");
        scanf("%d", &nim);
        fprintf(fp, "%d\n", nim);
    }
    fclose(fp);
    printf("\nProses Berhasil Ditulis.");
    */
    return 0;
}